# W31 优化实现方案（基线 0.10.1 / `2a0018e`，目标 0.10.2）

W30a–c 已进仓。下一刀 **不再改缠论公式和默认开关**，只处理：文档表缺行、手动 scan 打到旧库、18:00/周日覆盖率验收。
**能运营解决的不写代码。**

```
W31a DESIGN 补 W30 里程碑行（纯文档）✅
W31b 报表/CLI 标明 DB 路径与 K 线末日（消灭「0 只快照」误判）✅
W31c 18:00 + 周日验收清单（以运维为主，代码只补失败可见）
```

---
## 现状（W31 开工时）
| 面 | 状态 |
|----|------|
| 默认综合分 | `SCORE=0` → 1.22.30 |
| 混合分 | 0.10.0 规则表，默认关；空标签有 INFO/WARNING |
| 6D 展示 | 实件已有列 +「未进综合分」 |
| DESIGN §6/§11 | 已与标定表对齐；§9 里程碑表补 W30/W31 行（W31a）|
| 换手 | 管线不传 `--backfill-days`；手动 QA 窗口停在 09-11、0 只快照——**是打到仓内旧库，不是快照丢了**（W31b 根治）|

---
## W31a — 文档一行 ✅
DESIGN §9 表补 W30 / W31 里程碑行。不改 AGENTS、不改 scorer。

## W31b — QA 必须带「打的是哪套库」 ✅
**实现（只读，不改 turn）**
- `MysteryDB.data_fingerprint()` → `{db_path, kline_max, as_of_max}`（两条 MAX 查询，异常吞掉返回部分指纹）。
- `_turnover_qa_line` 第一行固定：
  `db=…/mystery_cache.db kline_max=YYYY-MM-DD as_of_max=YYYY-MM-DD`
- WARN 规则（只提示不改判）：
  - `kline_max` 为 None → 「库内无日K——空库/库不对（检查 MYSTERY_DB_PATH）」
  - `kline_max < trade_date` → 「K线止于 X，早于分析日 Y（未跑当日 sync？）」
  - `as_of_max` 为 None → 「股本快照表为空——可能未用 MYSTERY_DB_PATH 指向生产库」
- CLI `scan` / `sync-turnover` 启动打同款一行（`_print_db_fingerprint`）。
- 单测 `tests/test_turnover_qa.py::test_db_fingerprint_line`（qa fixture 断言 head 含路径/两日期、无 WARN）与 `test_db_fingerprint_warns_on_stale_and_empty`（旧库 WARN K线止于 + 快照表空；空库 WARN 无日K）。
**不做**：自动切库；扫描时重跑 sync。

## W31c — 覆盖率验收（主路径是 cron）
代码已够。本迭代只规定数字（weekly_shares.sh 开始行补 `echo db=`）：

| 时点 | 通过条件 |
|------|----------|
| 今日 18:00 后 | `pipeline_status.json`：`turnover_derive=ok`；日报 QA head `kline_max=2026-09-15`；自选 `n_with_shares≈86`；`fillable_rows>0` 则 `fillable_coverage` 不再是 0% |
| 可填未写 | `calc_float=0` 且 `fillable_rows>0` → 已有 exit 1（W29），保持 |
| 周日 `weekly_shares.sh` | `market_with_shares≥1000` 否则红（W28b）；达标后自动 `--backfill-days 20`（W29），已有 |
| 约 20 个交易日 | 自选窗口覆盖率接近可填覆盖率 |

**若 18:00 后仍 `n_with_shares=0`：先对 `echo $MYSTERY_DB_PATH` 与 QA 里的 `db=`，不要清表、不要改 `chip_low`。**

---
## 明确不做
- 改 `0.55/0.25/0.20` 或 `chan_score` 规则表
- 生产默认 `MYSTERY_CHAN_SCORE=1`
- `date < as_of` 回填
- 自研缠论 / 猜列扩除权
- 18:00 全市场竞价
- 为冲 95% 把 as_of 前空根算进已覆盖

## 验收
```bash
pytest -m "not integration"          # 155 passed（153 + W31b 2 项）
# 未设 env：SCORE=0，rule_ver=mystery-1.22.30-compat（不动）
# 日报 QA 第一行含 db= 与 kline_max=
# daily_pipeline 仍不传 --backfill-days
```

**一句话**：0.10.2 只让「扫的是哪套库」写在页脚上，并按今晚管线验收换手；缠论侧冻结。手动 QA 窗口停在 09-11 先当用错库，不要当快照丢了。
