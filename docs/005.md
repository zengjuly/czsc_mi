# Hermes：换手 0、日更扫描、平台文案、chip_quiet 列

只改 `/home/ai/ai_runner/stock/czsc_mi`。不要读旧仓、不要搜网、不要改评分。  
每个文件打开一次，改完就过。

---

## 2. 换手 0 ≠ 缺失

**文件：** `mystery/services/analyze.py` 里 `_avg_turnover_20`

现在：`if b.turnover` → `0` 被丢掉。

改成：

```python
vals = []
for b in daily.bars[-20:]:
    t = b.turnover
    if t is None:
        continue
    try:
        vals.append(float(t))
    except (TypeError, ValueError):
        continue
if not vals:
    return None
return round(sum(vals) / len(vals), 4)
```

`0` 计入均值。20 根都是 `None` 才返回 `None`（unknown）。

`classify` 已是 `turnover_20 is None` 才 unknown，**不要**再把 `0` 当缺失。

单测：`tests/test_scan_signals.py` 或 analyze 小测——`turnover_20=0` → `chip_low_unknown=False`，再按低位门判定 low/quiet。

---

## 3. 日更扫描不要和日报绑死

**文件：** `scripts/daily_pipeline.sh`

不要把全市场扫描塞进日报。两段分开：

```bash
# 日报（自选）
czsc-mi sync --period daily --days 365
czsc-mi daily --watchlist

# 全市场扫描（可选，默认关）
if [ "${RUN_MARKET_SCAN:-0}" = "1" ]; then
  czsc-mi scan --limit "${SCAN_LIMIT:-100}"
fi
```

- 默认：`RUN_MARKET_SCAN=0`，真三振池昨天的全市场 job 还在就继续显示，没有则页面提示「请运行 `czsc-mi scan --limit 100`」，**不要**用自选扫描冒充全市场。
- 真三振池只读 `latest_scan_job` 即可，不改表结构。

不要新增 CLI 子命令。`scan --watchlist` 保留给人手动扫自选。

---

## 4. 平台文案对表后再收 HINTS

**先本地查，不要猜：**

```bash
rg -n "平台状态" mystery/core/platform.py mystery/core/mystery_rules.py
```

只把**代码里真实会写入**的状态句收到 HINTS。常见应只留低位语义，例如「平台内」「横盘」。  
**删掉**过于宽的「离场」「未突破」（会把「已突破离场」算低位）。

对不上真实句 → 平台门不过，只走 `price_pos >= 0.15`。

改 `_PLATFORM_LOW_HINTS` 后跑已有 5 个 classify 用例；若「平台内缩量」那条用的字和源码不一致，把 fixture 改成源码里的原句，不要改平台引擎。

有现成 `tests/fixtures/gold_sh600519.json` 就看里面的 `平台状态`，与 HINTS 对照一行即可，不必再跑全市场。

---

## 5. 展示 chip_quiet

`--signal` **不要**加 `chip_quiet`（按你的要求：不必进 chip_low 过滤）。

只加列：

- `mystery/apps/web/app.py` 扫描表：增加「高位缩量」，`chip_quiet=True` 显示「是」，否则「否」。
- `mystery/apps/cli.py` `_cmd_scan` 打印里带上 `quiet`（有则标记）。
- Excel 扫描结果若走同一列 dict，加一列即可；没有扫描 sheet 就只改 Web/CLI。

`filter_by_signal(..., "chip_low")` 仍只认 `chip_low`。

---

## 不要做

- 不改 `CHIP_LOW_TURNOVER` / `CHIP_LOW_RETRACE`
- 不改综合分、不开 `CHAN_SCORE`
- 不 `fetch_bars`、不重算 VAP
- 不把 pipeline 默认改回全 A scan

---

## 提交与验收

```
fix: 换手0计入缩量；日更扫描与日报分离；平台HINTS对表；扫描表高位缩量列
```

```bash
pytest -q tests/test_scan_signals.py
```

手测：`turnover_20=0` 的票 unknown 为否；`daily_pipeline.sh` 默认不跑全市场 scan。


