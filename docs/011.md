# W30 改进方案（基线 0.10.0 / `5fe22f4`，目标 0.10.1）

阶段 6C 与 W20–W29 已在仓内收口。下一步 **不改权重、不默认打开混合分、不回填 `date < as_of`、不自研第二套缠论**。
两条并行线：A 把 0.10.0 用稳；B 把换手窗口真正铺开。不再开一轮「加深缠论」。

```
W30a DESIGN §11 / README 与 0.10.0 对齐（纯文档）
W30b 混合分回归与信号空标签可观测
W30c 6D 日报列实核 + 分关金标再跑
W30d 换手：盯 18:00 可填根 + 周日铺盘验收（运营为主）
```

## 1. W30a 文档还债
- DESIGN §11：浅规则已替换为 010 §9 表；买卖点/背驰仅 `SCORE=1` 进分；生产默认关。
- README：补「打开 `MYSTERY_CHAN_SCORE=1` 后 `rule_ver=mystery-0.10.0-chan`，不要和 1.22.30 金标比绝对值」。
- AGENTS §2.5 已在 `a64106b` 落地，只对读。
- 不做：改 `scorer.py`。

## 2. W30b 混合分可观测（仍默认关）
问题：`signal_flags` 走 czsc `_native.call_signal`，失败即空字符串，进分当 0，日志看不出。
- `signal_flags` 返回附 `signals_ok`（全部信号函数正常返回即 True）。
- `analyze_one_stock` 在 `SCORE=1` 时打一行 `bs= / div= / signals_ok=`。
- 离线单测：mock 无信号 → 分与「无标签」规则一致；mock 二买+底背驰 → 按表加分（+8+10）。
- 不做：信号失败不当结构失败；不默认开 SCORE。

## 3. W30c 6D 与金标再核
```bash
pytest -m "not integration"           # 全过
# 分关：金标三只与 1.22.30-compat 行为一致，rule_ver=mystery-1.22.30-compat
# 分开：rule_ver=mystery-0.10.0-chan，年线否决股混合分=0
```
打开一份 `scan --watchlist --report`（SCORE=0）实件核对：
末笔 / 中枢位置 / `bs_flag` / `divergence` 列存在、空为 `-`、有「未进综合分」标注、排序与 Mystery 分一致。
缺列才补展示；**禁止**默认扫描按混合分排序。

## 4. W30d 换手（主杠杆是运营）
| 时点 | 验收 |
|------|------|
| 18:00 后 | 自选 `fillable_coverage` 从 0% 抬起；`turnover_derive=ok`；可填未写必须 fail |
| 周日 `weekly_shares.sh` | `market_with_shares ≥ 1000` 否则 cron 红 |
| 铺盘成功后 | 自动 `--backfill-days 20` 已挂；看 `fillable_coverage` 而非窗口 46% |
| 约 20 个交易日 | 窗口覆盖率接近可填覆盖率；`chip_low_unknown` 下降 |

若 18:00 后可填覆盖率仍 0%：先查点号 `code` 映射、当日 volume、`pipeline_status`，不改 `chip_low` 阈值。

## 5. 明确不做
再改权重 / 生产默认 `score: true` / `date < as_of` 回填 / 自研缠论 / 猜列扩除权 WHERE / 18:00 全市场竞价 / Vue-FastAPI / 成分股算板块。

## 6. 验收清单
- 未设 env：`chan_enabled=True`，`chan_score_enabled=False`，金标三只分差 ≤ 1（今日三只因年线滤网行情漂移全 0——与基线代码对拍一致即可，非回归）
- `SCORE=1`：`rule_ver=mystery-0.10.0-chan`，否决股 0，信号空时有日志
- DESIGN §11 不再出现「±10/±5/±8、无买卖点」旧描述
- 日报 QA 仍分自选/全市场，且含可填口径
- `daily_pipeline.sh` 仍不传 `--backfill-days`
